# Backtests, the text menu and the unit tests need NOTHING from this file --
# they run on the Python standard library alone.

# Full-screen TUI. Recommended, and the only real dependency.
#   pip install textual
textual>=0.60

# Optional extras, install only what you need:
#   pip install ccxt     exchanges other than Binance, and real CEX orders
#   pip install PyYAML   stricter config.yaml parsing (a built-in reader is
#                        used when PyYAML is missing)
#   pip install solders  real Solana swaps through Jupiter
