# Dependency untuk mode paper trading LIVE (ambil harga real-time dari exchange).
# Mode backtest & unit test TIDAK butuh ini — jalan pakai Python standar.
ccxt>=4.2.0
PyYAML>=6.0.1

# TUI full-screen (opsional tapi disarankan)
textual>=0.60

# hanya untuk REAL swap Solana via Jupiter (opsional, eksperimental)
# solders>=0.21
