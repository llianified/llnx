# Runtime state & output (never commit these)
state.json
state.json.tmp
trades.csv
orders.jsonl
STOP

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# API keys — NEVER commit these
.env
*.key
secrets.*
